# portfoliodev

This project contains a simple black-and-white portfolio hero illustration.

Preview locally with:

```
# from project root
npx live-server --port=8080 --open=./index.html
```

The hero is implemented in `index.html` with styles in `styles.css` and uses an inline SVG for the PC setup and the "FOLIO" rug.